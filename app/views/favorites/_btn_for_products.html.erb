<% if item.favorited_by?(current_user) %>
  <%= button_tag item_favorites_path(item), method: :delete, data: { turbo_method: :delete }, class: "favorite-link" do %>
    <i class="fa fa-heart like-btn_products"></i>
  <% end %>
<% end %>